

import { Stack } from "@mui/material"
import { observer } from "mobx-react-lite"
import { PropsWithChildren } from "react"

export type CurrentAssetsLiabilitiesProps = PropsWithChildren

export const CurrentAssetsLiabilities = observer(function CurrentAssetsLiabilities(props: CurrentAssetsLiabilitiesProps) {
    
    return <Stack>
        
    </Stack>
})

export default CurrentAssetsLiabilities